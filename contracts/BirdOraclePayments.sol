pragma solidity ^0.5.16;

//slowly build this contract.
contract BirdOraclePayment {
    uint256 public aaa = 10;

    function isApproved() public pure returns (bool) {
        //address _addr
        return false;
    }
}
